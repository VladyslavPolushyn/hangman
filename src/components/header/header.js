import React from 'react';
import './header.sass';

const Header = () => {
  return(
    <header className="container main-header">
      <h1 className="text-center">Виселица</h1>
    </header>
  );
}

export default Header;